# 🧠 The Orchesity Neural-Core: Architecture

> **Status**: BATTLE TESTED (HARD MODE)
> **Generated By**: OrchesityAI Backend Compiler

---

## 🏛️ System Design

This project represents a **Neuro-Symbolic** approach to system design, combining the deterministic reliability of **Symbolic AI** (COBOL Logic) with the interpretive power of **Neural AI** (GPT-4o).

### 1. The Core (Symbolic Engine)
*   **File**: `src/main.cob`
*   **Language**: COBOL (GnuCOBOL)
*   **Logic Depth**: 4-Stage Waterfall (Age -> Income -> Credit Score -> Debt-to-Income)
*   **Role**: Executes business rules with 100% mathematical precision. It is the "Source of Truth".

### 2. The Bridge (Neural Interface)
*   **File**: `src/bridge/server.js` & `src/bridge/ai_agent.js`
*   **Language**: Node.js / Express
*   **Role**:
    *   Wraps the COBOL binary in a modern REST API.
    *   Intercepts `stdin` / `stdout` streams for real-time analysis.
    *   Interacts with OpenAI GPT-4o to "explain" the execution path.

### 3. The Knowledge Graph (Long-Term Memory)
*   **Database**: PostgreSQL
*   **Role**: Stores every transaction attempt, result, and the AI-generated "Logic Trace" for auditing purposes.

### 4. The Cortex (Short-Term Memory)
*   **Cache**: Redis
*   **Role**: Caches "Logic Traces". If a similar user profile appears (e.g., "Young High Income"), the AI doesn't need to re-think; it retrieves the trace from Redis in <1ms.

---

## 🔮 The "God's Eye" Capability

The unique feature of this architecture is **Self-Observation**.
Because the backend was compiled by OrchesityAI with specific hooks (using the Neuro-Symbolic Logic theory):

1.  **Traceability**: Every COBOL `IF/ELSE` branch is observable.
2.  **Topology Mapping**: We can aggregate individual traces into a **System-Wide Logic Map** (as seen in `tests/topology_mapper.js`).
3.  **Forensics**: We can ask the AI "Why did this specific rule trigger?", and it can reverse-engineer the exact COBOL line number responsible.

This transforms the "Black Box" into a "Glass House".
